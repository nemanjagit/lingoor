import { ShortDefinitionPipe } from './short-definition-pipe';

describe('ShortDefinitionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortDefinitionPipe();
    expect(pipe).toBeTruthy();
  });
});
